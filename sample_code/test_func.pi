#adder(a, b) {
    return a + b;
}

#subber(a, b) {
    return a - b;
}

#multer(a, b) {
    return a * b;
}

#adder4(a, b) {
    x = 1 + 3;
    return a + b + x;
}

#main() {
    x = adder(2, 7);
    y = subber(5, 3);
    z = multer(3, 7);
    q = diver(6, 5);
    k = adder4(2, 3);

    print(x, y, z, q, k);
}

#diver(a, b) {
    return a / b;
}