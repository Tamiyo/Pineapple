// This is a comment lul
// Thinking about immutable data types as default...
#fibo(n: int32): int32 {
    if (n <= 1) {
        return n;
    } else {
        return fibo(n - 1) + fibo(n - 2);
    }
}

x: int32 = fibo(9);
print(x);